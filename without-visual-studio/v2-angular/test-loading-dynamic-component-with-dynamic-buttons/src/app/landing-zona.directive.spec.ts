import { LandingZonaDirective } from './landing-zona.directive';

describe('LandingZonaDirective', () => {
  it('should create an instance', () => {
    const directive = new LandingZonaDirective();
    expect(directive).toBeTruthy();
  });
});
