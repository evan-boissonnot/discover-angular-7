import { EmptyNotEnabledDirective } from './empty-not-enabled.directive';

describe('EmptyNotEnabledDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyNotEnabledDirective();
    expect(directive).toBeTruthy();
  });
});
